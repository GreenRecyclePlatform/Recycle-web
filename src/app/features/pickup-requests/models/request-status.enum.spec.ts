import { RequestStatusEnum } from './request-status.enum';

describe('RequestStatusEnum', () => {
  it('should create an instance', () => {
    expect(new RequestStatusEnum()).toBeTruthy();
  });
});
