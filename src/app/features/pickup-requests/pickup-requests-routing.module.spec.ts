import { PickupRequestsRoutingModule } from './pickup-requests-routing.module';

describe('PickupRequestsRoutingModule', () => {
  it('should create an instance', () => {
    expect(new PickupRequestsRoutingModule()).toBeTruthy();
  });
});
